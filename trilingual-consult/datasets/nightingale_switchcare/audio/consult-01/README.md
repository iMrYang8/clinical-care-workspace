# consult-01 synthetic TTS

Generated by `../scripts/synthesize_consult_01_audio.sh`. **Not** clinic audio and **not** a quality claim.

- macOS `say`: Samantha (EN and the Malay sentence) and Ting-Ting (ZH). There is no Malay system voice here.
- WAV files are gitignored. Re-run the script locally.
- `python -m trilingual_consult.eval_audio` reports `ASR_UNAVAILABLE` until `LOCAL_ASR_MODEL_DIR` points at a real model **and** a hypothesis JSON is supplied. It will not invent a PolyWER number.
